services:
  - type: web
    name: py-browser-agent
    env: python
    plan: free
    # ↓ 只改这一行，追加 playwright 安装
    buildCommand: pip install -r requirements.txt && playwright install chromium
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port 8080
